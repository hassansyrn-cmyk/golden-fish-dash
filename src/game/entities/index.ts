/**
 * Game Entities (Fish, Obstacle, PowerUp, Particle, etc.)
 * Phase 1: Skeleton
 */
export interface BaseEntity {
  x: number;
  y: number;
  active: boolean;
}

// Future: export class Fish implements BaseEntity { ... }
// export class Obstacle implements BaseEntity { ... }
